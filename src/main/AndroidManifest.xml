<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions required by AutoPhone accessibility layer -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".AutoPhoneApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoPhone">

        <!-- Main Launcher Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AutoPhone">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
             AutoPhone AIDL Service
             Provides remote access to AutoPhone tools for Forge OS integration.
             Forge OS binds to this service to call automation methods.
        -->
        <service
            android:name=".AutoPhoneService"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.forge.autophone.IAutoPhoneService" />
            </intent-filter>
        </service>

        <!--
             AutoPhone Accessibility Service
             Forge OS accessibility layer for UI inspection, gesture dispatch,
             and programmatic text input across any foreground app.
        -->
        <service
            android:name=".AutoPhoneAccessibilityService"
            android:exported="true"
            android:label="@string/autophone_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/autophone_accessibility_config" />
        </service>

        <!--
             AutoPhone Notification Listener Service
             Allows reading, dismissing, and replying to notifications.
             Requires user to enable in Settings > Notifications > Notification Access.
        -->
        <service
            android:name=".service.AutoPhoneNotificationListener"
            android:exported="true"
            android:label="AutoPhone Notifications"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
